import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Imagem de produção enxuta (compose.prod.yaml / Dockerfile) — TD-07.5
  output: "standalone",
  /* config options here */
};

export default nextConfig;
